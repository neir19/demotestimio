serenity {
  driver = chrome
  webdriver {
    driver = chrome
    capabilities {
      browserName = "chrome"
      "goog:chromeOptions" {
        args = ["--start-maximized", "--disable-notifications"]
      }
    }
    }
}

pages {
    home = "https://demo.testim.io/"
}
